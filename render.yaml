services:
  - type: static
    name: vijay-solar
    buildCommand: npm run build
    staticPublishPath: ./build
    routes:
      # Rewrites all requests to index.html to support single-page client-side routing
      - type: rewrite
        source: /*
        destination: /index.html
